WMS.module('Owner', function(Owner, WMS, Backbone, Marionette, $, _) {  Owner.Router = Marionette.AppRouter.extend({    appRoutes: {      "owner": "showOwner",    },    header:'owner',    permissionsMap: {      'showOwner':'owner:retrieve'    }  });    var controllers = Owner.controllers = {}    , API = {    showOwner: function() {      if (!controllers.show) {        controllers.show = new Owner.Show.Controller();      }      controllers.show.showOwner();    },  };    WMS.on('owner', function() {    WMS.navigate('owner');  });    WMS.on('before:start', function() {    new Owner.Router({      controller: API    });  });});